{
    "name": "Python Project",
    "dockerFile": "Dockerfile",
    "forwardPorts": [5000],
    "postCreateCommand": "pip install -r requirements.txt",
    "extensions": ["ms-python.python"]
}
